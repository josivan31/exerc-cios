let titulo = "programação";
let linguagem = " em Javascript";
//console.log(titulo+linguagem); //concatenação no console log 
let fusao = titulo + linguagem;
console.log(fusao);

// length ->retorna o tamanho da mensagem
let mensagem = "olá, Bluemers";
console.log(`O tamanho da String mensagem é ${mensagem.length}`)

console.log(mensagem.length);